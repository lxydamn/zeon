version: "3.8"
services:
  zeon-register:
    image: zeon-register
    container_name: zeon-register
    ports:
      - "8000:8000"
    restart: always
    environment:
      - SPRING_REGISTER_URL=http://localhost:8000/eureka
